# Checklist

- [x] 已定义可本地安装的纯命令行工具形态，并明确使用 `uv` 管理项目
- [x] 已定义命令行配置高德 API Key 的能力，并明确配置按操作系统规范持久化保存
- [x] 已定义缺少配置时的错误反馈行为，确保用户知道如何补全配置
- [x] 已定义 `route` 命令的核心参数与输出要求，接口风格对齐官方 CLI，且不包含 `--json` 输入支持
- [x] 已定义 `search-poi` 命令的核心参数与输出要求，接口风格对齐官方 CLI，且不包含 `--json` 输入支持
- [x] 已定义统一的 JSON 输出结构，便于 Agent 解析成功和失败结果
- [x] 已定义 Agent 配套 skill 文档的交付要求
